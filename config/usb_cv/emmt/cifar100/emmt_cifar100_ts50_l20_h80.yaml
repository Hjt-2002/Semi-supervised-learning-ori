algorithm: emmt
save_dir: ./saved_models/usb_cv/
save_name: emmt_cifar100_ts50_l20_h80
resume: True
load_path: ./saved_models/usb_cv/emmt_cifar100_ts50_l20_h80/latest_model.pth
overwrite: True
use_tensorboard: False
use_wandb: True
epoch: 200
num_train_iter: 204800
num_log_iter: 256
num_eval_iter: 2048
batch_size: 8
eval_batch_size: 16
num_warmup_iter: 5120
num_labels: 200
uratio: 1
ema_m: 0.0
hard_label: True
T: 0.5
p_cutoff: 0.95
ulb_loss_ratio: 1.0
img_size: 32
crop_ratio: 0.875
optim: AdamW
lr: 0.0005
layer_decay: 0.5
momentum: 0.9
weight_decay: 0.0005
amp: False
clip: 0.0
use_cat: True
net: vit_small_patch2_32
net_from_name: False
data_dir: ./data
dataset: cifar100
train_sampler: RandomSampler
num_classes: 100
num_workers: 4
seed: 0
world_size: 1
rank: 0
multiprocessing_distributed: False
dist_url: tcp://127.0.0.1:10006
dist_backend: nccl
gpu: 1
use_pretrain: True
pretrain_path: https://github.com/microsoft/Semi-supervised-learning/releases/download/v.0.0.0/vit_small_patch2_32_mlp_im_1k_32.pth
# K-Means聚类相关参数
n_clusters: null  # 如果为null，则使用类别数作为聚类数
distance_ratio: 0.5  # 距离比例阈值（已废弃，保留用于兼容性）

# EMMT动态阈值调整参数
init_distance_percentile: 0.5  # 初始distance阈值百分位数，用于初始化每个聚类的distance阈值（0.5表示50%分位数）
consistency_threshold: 0.5  # 预测一致性阈值，超过此值才赋伪标签（0.5表示50%）
threshold_decay_low: 1.1  # 低一致性时的因子（阈值 × ）
consistency_threshold1: 0.2
threshold_decay_mid: 1.0  # 中等一致性时的因子（阈值 × ）
consistency_threshold2: 0.8
threshold_grow: 0.9  # 高一致性时的因子（阈值 × ）
 